# 提交 2130：Spark: Move 3.5 as 4.0

## 提交信息

- **序号**：2130 / 4088
- **哈希**：71cbe17cea5cac567920779de924eba62432934b
- **短哈希**：71cbe17ce
- **日期**：2025-05-14 09:49:29 -0700
- **作者**：huaxingao
- **提交说明**：Spark: Move 3.5 as 4.0
- **PR/Issue**：无 PR 号（直接提交到 main）

## 总体目的

本提交是 Iceberg 支持 Spark 4.0 的第一步，将 `spark/v3.5` 目录整体重命名为 `spark/v4.0`。这是一个纯 git 重命名操作，572 个文件被移动，内容完全不变（0 insertions, 0 deletions）。此操作的目的是以 Spark 3.5 的代码为基础创建 Spark 4.0 模块，后续提交将在 v4.0 模块中进行 Spark 4.0 适配修改，同时通过下一个提交将 v3.5 恢复。

## 如何达成设计目的

1. 使用 `git mv spark/v3.5 spark/v4.0` 将整个目录重命名
2. 所有文件内容保持不变，仅路径中的 v3.5 变为 v4.0

## 修改详情

### `spark/v3.5/**` -> `spark/v4.0/**` (重命名, 572 files, +0/-0 lines)

**修改目的**：将 Spark 3.5 模块重命名为 Spark 4.0 模块。

**工作逻辑**：纯目录重命名操作，涉及 572 个文件，包括 build.gradle、Java/Scala 源代码、测试文件、资源文件等。文件内容完全不变，只有目录路径从 `spark/v3.5/` 变为 `spark/v4.0/`。

## 总结

本提交是 Spark 4.0 支持的第一步，将 spark/v3.5 目录整体重命名为 spark/v4.0，作为 Spark 4.0 适配的代码基础。这是一个纯重命名操作，不改变任何文件内容，为后续的 Spark 4.0 适配工作做准备。
