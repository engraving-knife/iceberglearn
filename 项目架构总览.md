# Apache Iceberg 项目架构总览

> 本文档描述 Apache Iceberg（1.4.x 分支）的整体架构、模块划分、分层设计与核心数据流，是各模块架构文档（`<模块>/架构与设计思路.md`）的顶层索引。

## 1. 项目定位

Apache Iceberg 是一个**面向海量分析型数据集的高性能开放表格式**。它将 SQL 表的可靠性与简洁性引入大数据领域，使 Spark、Trino、Flink、Presto、Hive、Impala 等引擎能够安全地并发操作同一张表。

核心价值：
- **快照隔离与乐观并发**：每次提交生成不可变快照，读写互不阻塞。
- **Schema 演化**：支持增删列、重命名、重排序，无需重写数据。
- **分区演化**：分区策略可随时间变更，历史数据保持原有分区。
- **时间旅行**：可查询任意历史快照的数据状态。
- **跨引擎兼容**：统一的元数据协议，多引擎并发读写无歧义。

## 2. 整体分层架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        引擎集成层（Engine Integration）              │
│  spark/v3.5  flink/v1.17  mr  pig  hive3  hive-runtime              │
└──────────────┬──────────────┬──────────────┬──────────────────────-─┘
               │              │              │
┌──────────────▼──────────────▼──────────────▼───────────────────────-┐
│                     Catalog / 元数据存储层                           │
│  REST Catalog  Hive Metastore  Glue  Nessie  JDBC  Snowflake        │
│  InMemoryCatalog                                                   │
└──────────────┬─────────────────────────────────────────────────────-┘
               │
┌──────────────▼─────────────────────────────────────────────────────-┐
│                    核心实现层（Core Implementation）                 │
│  iceberg-core：表元数据、快照管理、扫描计划、提交协议、文件跟踪      │
└──────────────┬─────────────────────────────────────────────────────-┘
               │
┌──────────────▼─────────────────────────────────────────────────────-┐
│                      API 契约层（API Contract）                      │
│  iceberg-api：Table/Catalog/Schema/PartitionSpec/Snapshot 等接口    │
└──────────────┬─────────────────────────────────────────────────────-┘
               │
┌──────────────▼─────────────────────────────────────────────────────-┐
│               文件格式层（File Format）                              │
│  parquet  orc  arrow  avro(core内)                                  │
└──────────────┬─────────────────────────────────────────────────────-┘
               │
┌──────────────▼─────────────────────────────────────────────────────-┐
│            云存储 / 文件 IO 层（Storage & FileIO）                   │
│  aws(S3)  azure(ADLS)  gcp(GCS)  aliyun(OSS)  dell(ECS)            │
└──────────────┬─────────────────────────────────────────────────────-┘
               │
┌──────────────▼─────────────────────────────────────────────────────-┐
│              公共基础层（Common & Bundled Libraries）               │
│  iceberg-common（Dyn*反射工具）  bundled-guava（重打包Guava）       │
└───────────────────────────────────────────────────────────────────-─┘
```

## 3. 模块总览

本项目共包含 21 个有源码的 Gradle 模块（不含多版本子模块），按职责分为 7 层：

### 3.1 公共基础层

| 模块 | Gradle 名称 | 职责 | 架构文档 |
|------|------------|------|---------|
| `common` | iceberg-common | 最底层公共工具：Dyn* 动态反射系列（DynFields/DynMethods/DynConstructors/DynClasses），用于跨版本引擎兼容 | [common/架构与设计思路.md](common/架构与设计思路.md) |
| `bundled-guava` | iceberg-bundled-guava | Guava 重打包到 `org.apache.iceberg.relocated` 命名空间，避免依赖冲突 | [bundled-guava/架构与设计思路.md](bundled-guava/架构与设计思路.md) |

### 3.2 API 契约层

| 模块 | Gradle 名称 | 职责 | 架构文档 |
|------|------------|------|---------|
| `api` | iceberg-api | 核心接口定义：Table、Catalog、Schema、PartitionSpec、Snapshot、Expression、ScanTask 等，是所有实现与集成的契约 | [api/架构与设计思路.md](api/架构与设计思路.md) |

### 3.3 核心实现层

| 模块 | Gradle 名称 | 职责 | 架构文档 |
|------|------------|------|---------|
| `core` | iceberg-core | Iceberg 格式的参考实现：表元数据管理（TableMetadata）、快照/提交协议（SnapshotProducer/MergingSnapshotProducer）、扫描计划（TableScan/ManifestReader）、REST Catalog、Avro 读写、加密、映射等 | [core/架构与设计思路.md](core/架构与设计思路.md) |
| `data` | iceberg-data | 面向库用户的读写入口：`GenericAppenderFactory`、`GenericDataReaderFactory`，桥接 core 与文件格式 | [data/架构与设计思路.md](data/架构与设计思路.md) |

### 3.4 文件格式层

| 模块 | Gradle 名称 | 职责 | 架构文档 |
|------|------------|------|---------|
| `parquet` | iceberg-parquet | Parquet 格式读写：Builder 模式入口、列式迭代器、向量化读取、行组过滤（Metrics/Dictionary/Bloom） | [parquet/架构与设计思路.md](parquet/架构与设计思路.md) |
| `orc` | iceberg-orc | ORC 格式读写：ORC schema 映射、类型转换、读写器 | [orc/架构与设计思路.md](orc/架构与设计思路.md) |
| `arrow` | iceberg-arrow | Arrow 向量化读取：复用 parquet 向量化底座，产出 ColumnarBatch | [arrow/架构与设计思路.md](arrow/架构与设计思路.md) |

### 3.5 云存储 / 文件 IO 层

| 模块 | Gradle 名称 | 职责 | 架构文档 |
|------|------------|------|---------|
| `aws` | iceberg-aws | AWS 集成：S3FileIO、GlueCatalog、DynamoDbCatalog、Lake Formation、S3 签名 | [aws/架构与设计思路.md](aws/架构与设计思路.md) |
| `azure` | iceberg-azure | Azure 集成：ADLSv2 FileIO | [azure/架构与设计思路.md](azure/架构与设计思路.md) |
| `gcp` | iceberg-gcp | GCP 集成：GCS FileIO | [gcp/架构与设计思路.md](gcp/架构与设计思路.md) |
| `aliyun` | iceberg-aliyun | 阿里云集成：OSS FileIO | [aliyun/架构与设计思路.md](aliyun/架构与设计思路.md) |
| `dell` | iceberg-dell | Dell ECS 集成：ECS FileIO | [dell/架构与设计思路.md](dell/架构与设计思路.md) |
| `nessie` | iceberg-nessie | Nessie Catalog 集成：引用感知（branch/tag）的元数据 Catalog | [nessie/架构与设计思路.md](nessie/架构与设计思路.md) |
| `snowflake` | iceberg-snowflake | Snowflake Catalog 集成：通过 Snowflake SQL 操作 Iceberg 表 | [snowflake/架构与设计思路.md](snowflake/架构与设计思路.md) |
| `hive-metastore` | iceberg-hive-metastore | Hive Metastore Catalog：将 Iceberg 表元数据映射到 HMS | [hive-metastore/架构与设计思路.md](hive-metastore/架构与设计思路.md) |

### 3.6 引擎集成层

| 模块 | Gradle 名称 | 职责 | 架构文档 |
|------|------------|------|---------|
| `spark` | iceberg-spark | Spark 引擎集成（多版本：v3.2/v3.3/v3.4/v3.5/v4.1）：SparkSession 扩展、DataSource V2、SQL 扩展、向量化读取 | [spark/架构与设计思路.md](spark/架构与设计思路.md) |
| `flink` | iceberg-flink | Flink 引擎集成（多版本：v1.15/v1.16/v1.17/v2.1）：Flink Table API/SQL、批流读写 | [flink/架构与设计思路.md](flink/架构与设计思路.md) |
| `mr` | iceberg-mr | MapReduce/Hive 集成：InputFormat/OutputFormat、序列化、谓词下推 | [mr/架构与设计思路.md](mr/架构与设计思路.md) |
| `pig` | iceberg-pig | Apache Pig 集成：Pig UDF（LoadFunc/StoreFunc） | [pig/架构与设计思路.md](pig/架构与设计思路.md) |
| `hive3` | iceberg-hive3 | Hive 3 专属扩展：向量化读取、Hive3 ObjectInspector | [hive3/架构与设计思路.md](hive3/架构与设计思路.md) |

### 3.7 辅助模块

| 模块 | Gradle 名称 | 职责 | 架构文档 |
|------|------------|------|---------|
| `delta-lake` | iceberg-delta-lake | Delta Lake → Iceberg 迁移工具：快照 Delta Lake 表为 Iceberg 表 | [delta-lake/架构与设计思路.md](delta-lake/架构与设计思路.md) |
| `aws-bundle` | iceberg-aws-bundle | AWS 依赖打包（shaded jar，简化部署） | — |
| `azure-bundle` | iceberg-azure-bundle | Azure 依赖打包 | — |
| `gcp-bundle` | iceberg-gcp-bundle | GCP 依赖打包 | — |
| `hive3-orc-bundle` | iceberg-hive3-orc-bundle | Hive 3 ORC 依赖打包 | — |
| `hive-runtime` | iceberg-hive-runtime | Hive 运行时聚合 jar | — |

## 4. 核心数据结构与协议

### 4.1 表元数据层级

```
Table (api)
 └─ TableMetadata (core)          ← 元数据 JSON 文件（metadata.json）
     ├─ Schema                    ← 表结构（含字段 ID）
     ├─ PartitionSpec[]           ← 分区规格（可多个，支持演化）
     ├─ SortOrder[]               ← 排序规则
     ├─ Snapshot[]                ← 快照列表
     │   └─ ManifestList          ← 快照指向的 manifest 清单文件
     │       └─ ManifestFile[]    ← 各 manifest 文件
     │           └─ ManifestEntry ← 数据/删除文件条目
     │               └─ DataFile / DeleteFile
     ├─ SnapshotLogEntry[]        ← 快照变更日志
     ├─ MetadataLogEntry[]        ← 元数据文件变更日志
     └─ Properties                ← 表属性
```

### 4.2 提交协议（Optimistic Concurrency）

Iceberg 采用**乐观并发控制**：
1. 读取当前 TableMetadata 作为 base
2. 构建 `UpdateRequirement`（校验条件）+ `MetadataUpdate`（变更操作）
3. 尝试原子写入新的 metadata.json 文件
4. 若 base 已过期（被其他提交覆盖），重试

```
UpdateRequirement（前提校验）
  ├─ AssertTableUUID
  ├─ AssertCurrentSchemaID
  ├─ AssertRefSnapshotID
  ├─ AssertLastAssignedPartitionID
  └─ AssertTableDoesNotExist
      ↕ 配合
MetadataUpdate（状态变更）
  ├─ AddSchema / SetCurrentSchema
  ├─ AddPartitionSpec / SetDefaultPartitionSpec
  ├─ AddSnapshot / SetSnapshotRef
  ├─ RemoveSnapshots / RemoveOldStatistics
  └─ SetProperties / SetLocation
```

### 4.3 快照与扫描

```
扫描流程:
  Table.newScan()
   → TableScan.plan()                    ← 扫描计划
     → 读取当前 Snapshot 的 ManifestList
     → 用 ManifestEvaluator 过滤 manifest（分区级）
     → 读取匹配 manifest 中的 ManifestEntry
     → 用 InclusiveMetricsEvaluator 过滤文件（指标级）
     → 返回 ScanTask 集合（FileScanTask / DataTask）
   → 引擎并行执行 ScanTask
     → 通过 FileIO 读取数据文件（Parquet/ORC/Avro）
```

## 5. 模块依赖关系

```
bundled-guava ←── common ←── api ←── core ←── data
                                ↑       ↑
                          parquet/orc/arrow
                                ↑
                    ┌───────────┼───────────┐
                    │           │           │
                  aws         azure        gcp     ... (云存储)
                    │           │           │
                    └─────┬─────┴─────┬─────┘
                          │           │
                       nessie    hive-metastore  ... (Catalog)
                          │           │
                          └─────┬─────┘
                                │
                    ┌───────────┼───────────┐
                    │           │           │
                  spark       flink        mr/pig/hive3
```

**依赖方向**：上层依赖下层，同层模块尽量解耦。`api` 是最核心的契约，`core` 是最核心的实现。

## 6. 多版本引擎集成策略

Spark 和 Flink 模块采用**多版本子模块**结构，每个引擎版本一个独立子模块：

- **Spark**：`spark/v3.2`、`spark/v3.3`、`spark/v3.4`、`spark/v3.5`、`spark/v4.1`
- **Flink**：`flink/v1.15`、`flink/v1.16`、`flink/v1.17`、`flink/v2.1`

设计原因：
1. 不同版本的 Spark/Flink API 不兼容，需要独立编译。
2. 每个子模块包含 `spark`（核心集成）、`spark-extensions`（SQL 扩展）、`spark-runtime`（运行时 jar）。
3. `mr` 模块通过 `DynMethods`/`DynConstructors` 反射机制兼容 Hive 2/3，`hive3` 模块提供 Hive 3 专属扩展。

## 7. 设计亮点与核心模式

| 模式 | 应用场景 | 说明 |
|------|---------|------|
| **Builder 模式** | Parquet/ORC 读写器、Catalog 初始化、REST 请求 | 链式构建复杂对象，支持灵活配置 |
| **访问者模式** | Schema 遍历、Avro/Parquet 类型映射 | `TypeVisitor`、`AvroSchemaWithTypeVisitor` 解耦类型遍历与处理 |
| **策略模式** | FileIO 适配、Catalog 实现 | 同一接口（`FileIO`/`Catalog`）多种实现（S3/GCS/OSS…） |
| **空对象模式** | Dyn* 工具的 `AlwaysNull`、Arrow 的 `ConstantVectorHolder` | 避免空指针分支，简化调用链 |
| **乐观并发** | 表提交协议 | `UpdateRequirement` + `MetadataUpdate` 二元模型 |
| **快照隔离** | 读写隔离 | 不可变快照 + manifest 列表，读不阻塞写 |
| **反射兼容** | 跨版本引擎集成 | `DynFields`/`DynMethods` 动态访问引擎内部 API |
| **SPI 加载** | FileIO/Catalog 工厂 | `ServiceLoader` 动态加载实现 |
| **不可变值对象** | TableMetadata/Snapshot/DataFile | 元数据对象不可变，保证一致性 |
| **懒加载** | BaseSnapshot 的 manifest 缓存 | transient 字段首次访问时加载，避免序列化开销 |

## 8. 关键流程速览

### 8.1 建表流程
```
Catalog.createTable(identifier, schema, spec, location)
  → 检查表不存在
  → 构建 TableMetadata（初始版本 v1/v2）
  → 通过 FileIO 写入 metadata.json
  → 在 Catalog（HMS/Glue/REST…）注册表
```

### 8.2 数据写入流程
```
table.newAppend() / table.newOverwrite() / table.newRowDelta()
  → DataWriteSession 写数据文件（Parquet/ORC）
  → 生成 ManifestFile（记录数据文件信息）
  → 构建 Snapshot（含 manifest list）
  → 生成 UpdateRequirement（校验 base 快照）
  → 原子提交新 metadata.json
```

### 8.3 数据读取流程
```
table.newScan().select(...).filter(expression)
  → TableScan.plan()
  → 读取 Snapshot → ManifestList → ManifestFile[]
  → 分区级过滤（ManifestEvaluator）
  → 文件级过滤（InclusiveMetricsEvaluator）
  → 返回 FileScanTask[]
  → 引擎并行读取（ParquetReader/ORCReader/ArrowReader）
```

### 8.4 REST Catalog 流程
```
RESTSessionCatalog.initialize()
  → HTTPClient 连接 REST Server
  → 获取 OAuth Token
  → 表操作通过 HTTP API（v1/config, v1/namespaces, v1/tables…）
  → 服务器端管理元数据，客户端通过 RESTSerializers 序列化
```

## 9. 构建与项目结构

- **构建工具**：Gradle（Groovy DSL），Java 8/11/17
- **根配置**：`build.gradle`、`settings.gradle`、`gradle.properties`
- **多版本控制**：通过 `sparkVersions`/`flinkVersions`/`hiveVersions` 系统属性选择性构建
- **代码风格**：Spotless（`./gradlew spotlessApply`）
- **测试**：JUnit 5，每个模块有 `src/test`

## 10. 各模块架构文档索引

| 层次 | 模块 | 文档 |
|------|------|------|
| 公共基础 | common | [common/架构与设计思路.md](common/架构与设计思路.md) |
| 公共基础 | bundled-guava | [bundled-guava/架构与设计思路.md](bundled-guava/架构与设计思路.md) |
| API 契约 | api | [api/架构与设计思路.md](api/架构与设计思路.md) |
| 核心实现 | core | [core/架构与设计思路.md](core/架构与设计思路.md) |
| 核心实现 | data | [data/架构与设计思路.md](data/架构与设计思路.md) |
| 文件格式 | parquet | [parquet/架构与设计思路.md](parquet/架构与设计思路.md) |
| 文件格式 | orc | [orc/架构与设计思路.md](orc/架构与设计思路.md) |
| 文件格式 | arrow | [arrow/架构与设计思路.md](arrow/架构与设计思路.md) |
| 云存储 | aws | [aws/架构与设计思路.md](aws/架构与设计思路.md) |
| 云存储 | azure | [azure/架构与设计思路.md](azure/架构与设计思路.md) |
| 云存储 | gcp | [gcp/架构与设计思路.md](gcp/架构与设计思路.md) |
| 云存储 | aliyun | [aliyun/架构与设计思路.md](aliyun/架构与设计思路.md) |
| 云存储 | dell | [dell/架构与设计思路.md](dell/架构与设计思路.md) |
| Catalog | nessie | [nessie/架构与设计思路.md](nessie/架构与设计思路.md) |
| Catalog | snowflake | [snowflake/架构与设计思路.md](snowflake/架构与设计思路.md) |
| Catalog | hive-metastore | [hive-metastore/架构与设计思路.md](hive-metastore/架构与设计思路.md) |
| 引擎集成 | spark | [spark/架构与设计思路.md](spark/架构与设计思路.md) |
| 引擎集成 | flink | [flink/架构与设计思路.md](flink/架构与设计思路.md) |
| 引擎集成 | mr | [mr/架构与设计思路.md](mr/架构与设计思路.md) |
| 引擎集成 | pig | [pig/架构与设计思路.md](pig/架构与设计思路.md) |
| 引擎集成 | hive3 | [hive3/架构与设计思路.md](hive3/架构与设计思路.md) |
| 辅助 | delta-lake | [delta-lake/架构与设计思路.md](delta-lake/架构与设计思路.md) |
