# Iceberg 源码中文注释规范

> 本文档整合了"注释内容规范"（写什么）与"构建合规规则"（怎么写才不报错）两部分。
> 基于为 Iceberg 项目（1.4.x 分支）批量添加中文注释后修复 `./gradlew build` 与 `./gradlew spotlessApply` 全部错误的实战经验整理而成。
> 目的：保证注释既专业准确、体现设计意图，又符合 Google Java Format、Checkstyle、Errorprone 的校验规则，且不破坏代码语义。

---

## 一、总则

1. **只改注释，不动代码**：添加注释时绝不修改类名、方法名、变量名、常量名、方法签名、注解。任何标识符的改动都会导致编译失败。
2. **保留原有 Javadoc 标签**：如果原方法已有 `@param`/`@return`/`@throws`/`@deprecated` 标签，不要删除或替换它们，只在其上方添加中文摘要。
3. **注释插入位置**：Javadoc 注释必须放在注解（`@Override`、`@SuppressWarnings` 等）**之前**，不能插在注解和方法声明之间。
4. **先验证后提交**：每次批量添加注释后，必须依次运行 `./gradlew spotlessApply` 和 `./gradlew build -x test -x integrationTest` 验证。

---

## 二、注释范围与处理原则

### 2.1 处理范围

- 仅处理 `src/main` 下的 Java/Scala 源码，**不处理** `src/test`、`build/` 生成代码。
- 保留文件顶部 ASF license 头不动，类注释加在 class/object/trait 声明前。

### 2.2 处理原则

- **不修改任何代码逻辑**，只增/改注释，保留原行结构。
- 若原方法已有英文 Javadoc：用中文 Javadoc **替换**它（不要中英重复堆叠）。但若原有 `@param`/`@return`/`@throws` 标签的参数名/描述准确，可保留这些标签，仅替换摘要描述部分为中文。
- 中文要专业、准确、简洁。引用类用 `{@link}`，代码片段用 `{@code}`。
- 注释里不要出现"我""你"等口语。
- 保持缩进与文件风格一致（Java 2 空格，Iceberg 风格）。

### 2.3 完成后产出

- 直接修改源文件。
- 完成给定文件集合后，回报：处理了哪些文件、每个文件加了几处注释、有无特殊情况。

---

## 三、文件级 / 类级注释内容规范

文件级 / 类级注释（Javadoc，置于 class/object/trait 声明前）必须包含：

- **所属模块**：如 iceberg-api / iceberg-core，及该模块在整体架构中的层次定位。
- **职责**：该类做什么，1~4 条要点。
- **设计意图**：为什么这样设计（模式、权衡、跨版本兼容、性能、线程安全等）。
- **上下游关系**：依赖谁、被谁使用（能判断则写，不确定写"被 xx 调用"）。

### 示例

```java
/**
 * 表事务的 core 实现：把多个表更新操作聚合为一次原子提交。
 *
 * <p>所属模块：iceberg-core。
 *
 * <p>职责：
 *
 * <ul>
 *   <li>实现 {@link Transaction}，在一个事务内串行执行多个 {@link PendingUpdate}。
 *   <li>通过 {@link TransactionTable} 提供事务内的临时表视图。
 *   <li>支持四种事务类型：CREATE_TABLE、REPLACE_TABLE、CREATE_OR_REPLACE_TABLE、SIMPLE。
 * </ul>
 *
 * <p>设计意图：
 *
 * <ul>
 *   <li>事务内每次操作 commit 只更新 current，不触碰底层 ops，保证事务内多操作可见性一致。
 *   <li>仅最后一次 commit 才真正向底层 ops 提交，并通过 CAS 重试处理并发冲突。
 * </ul>
 *
 * <p>上下游关系：由 {@link Transactions} 工厂创建；提交时调用底层 {@link TableOperations#commit}。
 */
public class BaseTransaction implements Transaction {
```

---

## 四、函数级注释内容规范

函数级注释（Javadoc，置于方法声明前）要求：

- **公有方法、复杂私有方法**都必须有中文注释。
- 内容：作用、关键参数、返回值、抛出异常、设计要点。
- **较长函数**（>15 行或有复杂分支/循环）：用 `<p>逻辑：` 段落详细解释内部执行步骤。
- 简单 getter/setter/toString 可只写一句中文说明。

### 示例

```java
  /**
   * 派生扫描计划总耗时的 {@link Timer}（单位：纳秒）。
   *
   * @return 扫描计划总耗时计时器
   */
  @Value.Derived
  public Timer totalPlanningDuration() { ... }
```

```java
  /**
   * 提交事务，将所有 PendingUpdate 一次性应用到底层表。
   *
   * <p>逻辑：遍历所有 update 逐一 applyUpdates 重新应用到最新元数据（因底层可能已被
   * 其他提交改变），通过 CAS 重试处理并发冲突，失败时清理未提交文件。
   *
   * @throws CommitFailedException 若并发冲突且重试耗尽
   */
  public void commitTransaction() { ... }
```

---

## 五、Javadoc 摘要句（MissingSummary）

Errorprone 的 `MissingSummary` 规则要求每个 Javadoc 注释的第一行必须是**摘要句**（summary fragment），不能直接以 `@param`/`@return` 等标签开头。

### ❌ 错误写法

```java
/** @return 结果数据文件计数器 */
public Counter resultDataFiles() { ... }
```

```java
/**
 * @param table 表对象
 * @return 快照列表
 */
public List<Snapshot> snapshots(Table table) { ... }
```

### ✅ 正确写法

```java
/** 返回结果数据文件计数器。 */
public Counter resultDataFiles() { ... }
```

```java
/**
 * 返回表的快照列表。
 *
 * @param table 表对象
 * @return 快照列表
 */
public List<Snapshot> snapshots(Table table) { ... }
```

### 规则

- 单行注释 `/** XXX */` 中 XXX 就是摘要，不要以 `@` 开头。
- 多行注释的第一段（`/**` 后、第一个空行前）必须是摘要句，以中文句号 `。` 结尾。
- 如果只需要描述返回值，写成 `/** 返回 XXX。 */` 即可，不需要 `@return` 标签。

---

## 六、禁止使用模板标签

批量添加注释时，严禁使用以下无意义模板标签——它们会与原有标签冲突，触发 Checkstyle `JavadocMethod`（重复标签/无用标签）错误：

### ❌ 禁止的模板写法

```java
/**
 * 返回当前快照的所有祖先快照迭代器。
 *
 * @param table 参数          ← 无意义模板，且与原 @param table 重复
 * @return Iterable<Snapshot> 返回值  ← 无意义模板，且与原 @return 重复
 *     <p>Returns an iterable that traverses...
 * @param table a Table       ← 原有标签
 * @return an iterable from...     ← 原有标签
 */
```

### ✅ 正确写法

```java
/**
 * 返回当前快照的所有祖先快照迭代器。
 *
 * <p>Returns an iterable that traverses...
 *
 * @param table a Table
 * @return an iterable from the table's current snapshot to its last known ancestor
 */
```

### 规则

- **绝不添加 `@param X 参数`**（描述仅为"参数"二字）——这是无信息量的模板。
- **绝不添加 `@return XXX 返回值`**（描述以"返回值"结尾）——这是无信息量的模板。
- 如果原方法已有英文 `@param`/`@return` 标签，保留原标签，只加中文摘要句。
- 如果原方法没有任何 Javadoc 标签，需要添加时，`@param` 的描述必须写明该参数的具体含义，`@return` 的描述必须写明返回值的具体含义。

---

## 七、Javadoc 标签顺序（AtclauseOrder）

Checkstyle `AtclauseOrder` 规则要求块标签按以下顺序排列：

```
@param → @return → @throws → @deprecated
```

### ❌ 错误写法

```java
/**
 * 已废弃：创建无类型常量持有者。
 *
 * @deprecated since 1.4.0，将在 1.5.0 移除    ← @deprecated 不能在最前面
 * @param numRows 行数
 * @param constantValue 常量值
 * @return 常量持有者
 */
```

### ✅ 正确写法

```java
/**
 * 已废弃：创建无类型常量持有者。
 *
 * @param numRows 行数
 * @param constantValue 常量值
 * @return 常量持有者
 * @deprecated since 1.4.0，将在 1.5.0 移除
 */
```

---

## 八、`@Deprecated` 与 `@deprecated` 配对（MissingDeprecated）

Checkstyle `MissingDeprecated` 规则要求：`@Deprecated` 注解和 `@deprecated` Javadoc 标签**必须同时存在或同时不存在**。

### ❌ 错误写法

```java
/** 返回 partitions。 */          ← 缺少 @deprecated 标签
@Deprecated
public static List<SparkPartition> getPartitions(...) { ... }
```

```java
/**
 * 已弃用：请使用 UncheckedIOException。
 *                                    ← 缺少 @deprecated 标签
 */
@Deprecated
public class RuntimeIOException extends UncheckedIOException { ... }
```

### ✅ 正确写法

```java
/**
 * 返回 partitions。
 *
 * @deprecated 使用 {@link #getPartitions(SparkSession, Path, String, Map, PartitionFilter)} 替代
 */
@Deprecated
public static List<SparkPartition> getPartitions(...) { ... }
```

### 规则

- 看到 `@Deprecated` 注解，必须在 Javadoc 中加 `@deprecated` 标签并说明替代方案。
- 看到 `@deprecated` Javadoc 标签，必须确认方法/类上有 `@Deprecated` 注解。
- `@deprecated` 标签放在 Javadoc 末尾（遵循标签顺序规则）。

---

## 九、Javadoc 中的尖括号 `< >` 处理

Checkstyle 的 Javadoc 解析器会将 `<X>` 视为 HTML 标签，导致 `MissingDeprecated`（解析错误）或 `JavadocStyle`（未闭合 HTML 标签）错误。

### 9.1 Javadoc 描述文本中的泛型

### ❌ 错误写法

```java
/** 可被 Java 序列化的 Map<Integer,ByteBuffer> 包装类。 */
```

```java
/** 把 key-value 映射转为 key->Set<value> 的 multimap。 */
```

### ✅ 正确写法

```java
/** 可被 Java 序列化的 {@code Map<Integer,ByteBuffer>} 包装类。 */
```

```java
/** 把 key-value 映射转为 key-&gt;Set&lt;value&gt; 的 multimap。 */
```

### 规则

- Javadoc 文本中出现泛型 `X<Y>` 时，用 `{@code X<Y>}` 包裹。
- 如果泛型已在 `{@code}` 内但仍报错，改用 HTML 实体：`&lt;` 和 `&gt;`。
- Javadoc 文本中出现 `<word>` 形式的占位符（如 `<table>`、`<表名>`、`<FILE_FORMAT>`、`<table-location>`），必须用 `&lt;`/`&gt;` 转义。

### 9.2 `{@link}` 中的泛型

### ❌ 错误写法

```java
/** 实现 {@link Consumer<ErrorResponse>} 接口。 */
```

```java
/** 返回 {@link Function<Object, Object>}。 */
```

### ✅ 正确写法

```java
/** 实现 {@link Consumer} 接口。 */
```

```java
/** 返回 {@link Function}。 */
```

### 规则

- **`{@link}` 标签内不要带泛型参数**。`{@link Consumer<ErrorResponse>}` 会触发 `no viable alternative at input '<'` 解析错误。
- 如需指明泛型类型，在描述文本中用 `{@code}` 补充说明：`返回 {@code Consumer<ErrorResponse>} 类型的处理器。`

### 9.3 `*/` 在注释文本中

### ❌ 错误写法

```java
/** 提供 write*/read* 私有方法。 */
```

`*/` 会被解析为 Javadoc 注释结束符，导致后续文本被当作 Java 代码。

### ✅ 正确写法

```java
/** 提供 write*&#47;read* 私有方法。 */
```

### 规则

- Javadoc 注释内出现 `*/` 字符串时，将 `/` 替换为 HTML 实体 `&#47;`。

---

## 十、`@throws` 标签格式

### 10.1 异常名必须是合法 Java 标识符

### ❌ 错误写法

```java
 * @throws E1/E2/E3 对应类型的受检异常     ← / 不是合法标识符字符
 * @throws E1、E2 或 E3 对应类型的受检异常   ← 、 不是合法标识符字符
```

### ✅ 正确写法

```java
 * @throws E1 第一种受检异常
 * @throws E2 第二种受检异常
 * @throws E3 第三种受检异常
```

### 规则

- `@throws` 后的异常名必须是单个合法 Java 标识符（字母、数字、下划线、`$`）。
- 多个异常类型必须拆分为多个 `@throws` 标签，每个标签一个异常。
- 不能用 `/`、`、`、`，` 等分隔符在单个 `@throws` 中列多个异常。

### 10.2 `@throws` 必须有非空描述

### ❌ 错误写法

```java
 * @throws IOException
```

### ✅ 正确写法

```java
 * @throws IOException 本实现不抛出异常，仅保留接口签名兼容
```

### 规则

- Checkstyle `NonEmptyAtclauseDescription` 要求 `@throws`/`@param` 等标签必须有非空描述。

---

## 十一、`@param` 参数名必须匹配

### ❌ 错误写法

```java
/**
 * @param partition 分区值          ← 方法参数实际叫 newPartition
 * @return 当前 Builder
 */
public Builder withPartition(StructLike newPartition) { ... }
```

```java
/**
 * @param expectedSchema 期望的 Schema   ← 方法参数实际叫 expected
 */
public static Schema buildAvroProjection(
    Schema schema, org.apache.iceberg.Schema expected, Map<String, String> renames) { ... }
```

### ✅ 正确写法

```java
/**
 * @param newPartition 分区值
 * @return 当前 Builder
 */
public Builder withPartition(StructLike newPartition) { ... }
```

### 规则

- `@param` 后的参数名必须与方法签名中的实际参数名**完全一致**。
- 添加注释前，必须先读方法签名确认参数名，不能靠猜测。

---

## 十二、注解与 Javadoc 的位置关系

### ❌ 错误写法（重复注解）

```java
  @Override                                    ← 原有注解
  /**                                          ← 注释插在注解和方法之间
   * 转换 notEq 谓词。
   */
  @Override                                    ← 又出现一次
  public <T> Action notEq(Bound<T> expr, Literal<T> lit) { ... }
```

这会导致 `@Override 不是可重复的注释类型` 编译错误。

### ✅ 正确写法

```java
  /**
   * 转换 notEq 谓词。
   */
  @Override
  public <T> Action notEq(Bound<T> expr, Literal<T> lit) { ... }
```

### 规则

- Javadoc 注释永远在注解**之前**：`Javadoc → 注解 → 方法/类声明`。
- 在已有注解的方法前插入 Javadoc 时，确保 Javadoc 放在所有注解之前，不要产生重复注解。
- 批量插入后，检查是否产生了 `@Override @Override` 或 `@SuppressWarnings @SuppressWarnings` 的重复。

---

## 十三、工具类构造器（HideUtilityClassConstructor）

Checkstyle `HideUtilityClassConstructor` 规则要求：只含静态方法的工具类必须有 `private` 构造器。

### ❌ 错误写法

```java
public class SnapshotUtil {

  /** 私有构造：工具类禁止实例化。 */    ← 只有 Javadoc，没有实际构造器

  public static Snapshot currentSnapshot(Table table) { ... }
}
```

### ✅ 正确写法

```java
public class SnapshotUtil {

  /** 工具类禁止实例化。 */
  private SnapshotUtil() {}

  public static Snapshot currentSnapshot(Table table) { ... }
}
```

### 规则

- 如果给工具类添加了"私有构造"的 Javadoc，必须同时添加 `private ClassName() {}` 构造器实现。
- 如果原工具类没有显式构造器（依赖默认 public 构造器），添加注释时不要写"私有构造"的 Javadoc，除非同时补上构造器代码。

---

## 十四、Spotless / Google Java Format 注意事项

### `*/` 在 Javadoc 中

```java
// ❌ google-java-format 解析失败：43:29: error: 需要 class, interface 或 enum
/** 提供 write*/read* 私有方法。 */

// ✅ 用 HTML 实体转义
/** 提供 write*&#47;read* 私有方法。 */
```

### 规则

- Spotless 使用 `google-java-format 1.7`，对 Javadoc 中的 `*/` 极其敏感。
- 任何在 Javadoc 注释内部出现的 `*/`（即使是 `write*/read*` 这种文本）都会被当作注释结束符。
- 解决方案：将 `/` 替换为 HTML 实体 `&#47;`。

---

## 十五、Checkstyle Javadoc 解析器注意事项

Checkstyle 的 Javadoc 解析器比标准 `javadoc` 工具更严格：

| 问题 | 触发规则 | 解决方案 |
|------|----------|----------|
| `<table>` 等尖括号文本 | JavadocStyle | 用 `&lt;table&gt;` 转义 |
| `<表名>` 等中文尖括号 | MissingDeprecated | 用 `&lt;表名&gt;` 转义 |
| `{@link Foo<Bar>}` | MissingDeprecated | 去掉泛型：`{@link Foo}` |
| `Map<X,Y>` 裸泛型 | MissingDeprecated | 用 `{@code Map<X,Y>}` 包裹 |
| `@throws E1/E2` | MissingDeprecated | 拆分为多个 `@throws` |
| `@throws IOException`（无描述）| NonEmptyAtclauseDescription | 补充描述 |
| `@param X 参数`（模板） | JavadocMethod | 删除或写有意义的描述 |
| `@deprecated` 在 `@param` 前 | AtclauseOrder | 调整为 `@param → @return → @throws → @deprecated` |

---

## 十六、自检清单

添加注释后，提交前逐项确认：

- [ ] **未修改任何代码标识符**（类名、方法名、变量名、常量名、参数名）
- [ ] **未修改任何方法签名**（参数列表、返回类型、throws 声明）
- [ ] **未修改任何注解**（`@Override`、`@Deprecated`、`@SuppressWarnings` 等）
- [ ] **未产生重复注解**（`@Override @Override`）
- [ ] Javadoc 注释在注解之前
- [ ] 每个 Javadoc 都有摘要句（不以 `@` 标签开头）
- [ ] `@param` 参数名与方法签名一致
- [ ] `@param`/`@return`/`@throws` 描述非空且有具体含义
- [ ] `@throws` 异常名是合法 Java 标识符
- [ ] 标签顺序为 `@param → @return → @throws → @deprecated`
- [ ] `@Deprecated` 注解与 `@deprecated` 标签成对出现
- [ ] Javadoc 中无裸泛型 `<X>`（用 `{@code}` 包裹或去掉）
- [ ] `{@link}` 内无泛型参数
- [ ] Javadoc 中无 `*/` 字符串（用 `&#47;` 转义）
- [ ] Javadoc 中无 `<word>` 形式的 HTML 标签（用 `&lt;`/`&gt;` 转义）
- [ ] 工具类有 `private` 构造器
- [ ] `./gradlew spotlessApply` 通过
- [ ] `./gradlew build -x test -x integrationTest` 通过

---

## 十七、常见错误速查

| 错误信息 | 根因 | 修复 |
|----------|------|------|
| `google-java-format: 需要 class, interface 或 enum` | Javadoc 中 `*/` 提前关闭注释 | `*/` → `*&#47;` |
| `找不到符号` | 类名/方法名/常量名被误改 | 从 git 恢复原标识符 |
| `方法不会覆盖或实现超类型的方法` | 方法名被误改 | 恢复原方法名 |
| `Override 不是可重复的注释类型` | Javadoc 前后各有一个 `@Override` | 删除 Javadoc 前的多余注解 |
| `[MissingSummary] A summary fragment is required` | `/** @return XXX */` 无摘要 | 改为 `/** 返回 XXX。 */` |
| `[JavadocMethod] 无用标签 @param` | `@param` 参数名不匹配或模板重复 | 修正参数名或删除模板标签 |
| `[JavadocMethod] 重复的 @return` | 模板 `@return` 与原有 `@return` 重复 | 删除模板 `@return` |
| `[AtclauseOrder] 阻止标签必须按顺序显示` | 标签顺序不对 | 重排为 `@param → @return → @throws → @deprecated` |
| `[MissingDeprecated] 必须同时含有 @Deprecated 注解以及 @deprecated 注释` | 二者缺一 | 补齐缺失的那个 |
| `[MissingDeprecated] no viable alternative at input '<'` | `{@link Foo<Bar>}` 中泛型 | 去掉 `{@link}` 内泛型 |
| `[MissingDeprecated] 缺少 HTML 闭合标签` | `<X>` 被视为 HTML 标签 | `&lt;X&gt;` 转义或 `{@code}` 包裹 |
| `[JavadocStyle] 未关闭的 HTML 标签` | `<table>` 等被视为 HTML | `&lt;table&gt;` 转义 |
| `[NonEmptyAtclauseDescription] @标签应有非空说明` | `@throws` 无描述 | 补充描述 |
| `[HideUtilityClassConstructor] 工具类应隐藏 public 构造器` | 工具类无 private 构造器 | 添加 `private ClassName() {}` |
